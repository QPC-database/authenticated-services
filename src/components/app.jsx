import React from 'react';
import styles from './app.scss';

const App = () => (
  <div className={styles.app}>
    <div className={styles.default}>React Starter Template</div>
  </div>
);

export default App;
